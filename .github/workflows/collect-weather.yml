name: Collect Weather Data

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest

    steps:
      - name: Call weather collect API
        run: |
          curl -f https://weather-app-chi-pied-98.vercel.app/api/collect
